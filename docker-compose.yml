services:
  db:
    image: mysql:9.6.0
    container_name: mysql_container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: gestion_inventario_coches
      MYSQL_USER: igrisdev
      MYSQL_PASSWORD: igrisdev123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
